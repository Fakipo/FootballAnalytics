import React from "react";


const Fixtures = (props) => {
    if(props == 'fixtures'){
        return(
            <div>
            Fixtures
        </div>
    );
}else{
    return ({});
}
};

export default Fixtures;